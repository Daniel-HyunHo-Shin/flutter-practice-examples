name: flashcard
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  cloud_firestore: ^4.5.2
  firebase_core: ^2.10.0
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.5
  go_router: ^6.5.7
  precached_network_image: ^1.1.4
  uuid: ^3.0.7

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
